如何解决load次数过多：
- 子句分割算法
	-- 图划分方法，切割的边数最少
- 可能的解保存下来
- 增加bin的大小会有什么影响
- 能否使用bin间学习子句
   -- 使用bram将全局学习子句保存下来
- 动态选择bin
-- 	使用类似vsids的方式



求解uf50-01.cnf
-- 大部分刚载入就出错，根本没有学习子句
-- 


将sat_engine使用wsat代替
-- 好处是什么
-- 如何回退


使用多个小bin并行搜索
-- 竞争关系：行不通，因为每个bin求解速度很快，不需要竞争。
-- 合作关系：空间划分，多个sat engine同时求解多个bin，但多个bin产生的值会发生冲突，这时冲突分析与回退比较难。
-- 同时搜索：各个bin进行通信，这种就和ASIC实现的那篇差不多了，相当于提高了bin的使用率，其实还是一个大bin。


如何实现增量式的sat
在考虑STP能对sat进行优化么。

